<UserControl x:Name="Main"  x:Class="Snapshot.GUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:Snapshot" d:DataContext="{d:DesignInstance Type=local:SnapshotVM}"
             xmlns:bgInterfaces="clr-namespace:BuzzGUI.Interfaces;assembly=BuzzGUI.Interfaces"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Margin" Value="1" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Margin" Value="2" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="3,2" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="5,0" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" MinWidth="200" MinHeight="200" Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <TreeView x:Name="MachineList" Grid.Row="0"  MinHeight="200" ItemsSource="{Binding States}">
                <TreeView.ItemContainerStyle>
                    <!-- This Style binds a TreeViewItem to a TreeViewItemViewModel. -->
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="FontWeight" Value="Normal" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:MachineStateVM}" ItemsSource="{Binding Children}">
                        <CheckBox IsThreeState="True" IsChecked="{Binding IsChecked}" Click="OnThreeStateClick">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding MachineName}" />
                            </StackPanel>
                        </CheckBox>
                    </HierarchicalDataTemplate>

                    <DataTemplate DataType="{x:Type local:MachineDataVM}">
                        <CheckBox IsChecked="{Binding IsChecked}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>

                    <HierarchicalDataTemplate DataType="{x:Type local:PropertyStateGroupVM}" ItemsSource="{Binding Children}">
                        <CheckBox IsThreeState="True" IsChecked="{Binding IsChecked}" Click="OnThreeStateClick">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </CheckBox>
                    </HierarchicalDataTemplate>

                    <HierarchicalDataTemplate DataType="{x:Type local:TrackPropertyStateGroupVM}" ItemsSource="{Binding Children}">
                        <CheckBox IsThreeState="True" IsChecked="{Binding IsChecked}" Click="OnThreeStateClick">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </CheckBox>
                    </HierarchicalDataTemplate>

                    <DataTemplate DataType="{x:Type local:PropertyStateVM}">
                        <CheckBox IsChecked="{Binding IsChecked}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock x:Name="txtSelCount" TextWrapping="Wrap" Text="{Binding SelectionInfo, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Row="1">
                <CheckBox x:Name="chkSelNew" Content="Select new machines" FontSize="14"/>
                <CheckBox x:Name="chkCaptureOnSlot" Content="Capture on slot change" />
                <CheckBox x:Name="chkRestoreOnSlot" Content="Restore on slot change" />
                <CheckBox x:Name="chkRestoreOnLoad" Content="Restore on song load" />
                <CheckBox x:Name="chkRestoreOnStop" Content="Restore on transport stop" />
                <Separator Height="9" />
                <Button x:Name="btnCapture" Command="{Binding cmdCapture}">Capture</Button>
                <Button x:Name="btnCaptureMissing" Command="{Binding cmdCaptureMissing}">Capture Missing</Button>
                <Separator Height="9" />
                <Button x:Name="btnRestore" Command="{Binding cmdRestore}">Restore</Button>
                <Separator Height="9" />
                <Button x:Name="btnClear" Command="{Binding cmdClear}">Clear</Button>
                <Button x:Name="btnPurge" Command="{Binding cmdPurge}">Purge</Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
